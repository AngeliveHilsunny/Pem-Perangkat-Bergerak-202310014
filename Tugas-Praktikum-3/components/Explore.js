import React from 'react';
import {
    View,
    Text,
    StyleSheet,
    Image,
    ScrollView,
    ImageBackground,
} from 'react-native';

const Explore = ({}) => {
    return (
        <View>
            <Text>Explore</Text>
        </View>
    );
};

export default Explore;