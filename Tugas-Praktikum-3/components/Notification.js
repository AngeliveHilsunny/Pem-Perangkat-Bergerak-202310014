import React from 'react';
import {
    View,
    Text,
    StyleSheet,
    Image,
    ScrollView,
    ImageBackground,
} from 'react-native';

const Notification = ({}) => {
    return (
        <View>
            <Text>Notification</Text>
        </View>
    );
};

export default Notification;